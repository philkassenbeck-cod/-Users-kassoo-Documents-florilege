// content/config.ts — réglages produit (app), hors noyau.

/**
 * Force `relier_compassion` : libellé « Accord » (défaut) vs « Bienveillance » (alt).
 * L'identifiant reste stable ; seul l'affichage change. À trancher / A-B tester.
 */
export const USE_ALT_COMPASSION_LABEL = false;
