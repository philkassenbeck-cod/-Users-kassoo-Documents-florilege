# Florilège — variables d'environnement

# Persistance 360 (optionnel). Sans ces variables, l'app utilise le store mémoire.
# Pour la production, appliquer supabase/schema.sql puis renseigner :
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
